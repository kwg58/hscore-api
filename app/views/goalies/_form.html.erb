<%= form_for(goaly) do |f| %>
  <% if goaly.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(goaly.errors.count, "error") %> prohibited this goaly from being saved:</h2>

      <ul>
      <% goaly.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :time_played %>
    <%= f.number_field :time_played %>
  </div>

  <div class="field">
    <%= f.label :goals_against %>
    <%= f.number_field :goals_against %>
  </div>

  <div class="field">
    <%= f.label :references %>
    <%= f.text_field :references %>
  </div>

  <div class="field">
    <%= f.label :references %>
    <%= f.text_field :references %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
